# ========================================
# API Router 环境变量配置示例
# ========================================

# API 配置
# 默认 API 密钥，可通过 Authorization 头覆盖
DEFAULT_API_KEY=your-api-key-here

# 日志配置
# 日志级别：trace, debug, info, warn, error
RUST_LOG=info
# 日志格式：text（人类可读）或 json（适合日志聚合）
LOG_FORMAT=text

# Sentry 错误追踪（可选）
# 取消注释以启用 Sentry 集成
# SENTRY_DSN=https://your-key@o1234567.ingest.sentry.io/9876543
# SENTRY_SAMPLE_RATE=1.0          # 采样率：0.0-1.0
# SENTRY_ENVIRONMENT=production   # 环境标签

# 速率限制（可选 - 也可在配置文件中设置）
# 每分钟允许的最大请求数
# RATE_LIMIT_REQUESTS_PER_MINUTE=120
# 允许的突发容量
# RATE_LIMIT_BURST=40

# 自定义配置文件路径（可选）
# 指定配置文件的完整路径，默认使用 transformer/ 目录下的配置
# API_ROUTER_CONFIG_PATH=/path/to/custom/config.json
